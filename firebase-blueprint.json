{
  "entities": {
    "SystemConfig": {
      "title": "System Configuration",
      "description": "Global application settings including the shared admin security PIN",
      "type": "object",
      "properties": {
        "adminPin": {
          "type": "string",
          "description": "Shared administrative PIN code",
          "minLength": 4,
          "maxLength": 32
        },
        "updatedAt": {
          "type": "string",
          "description": "ISO timestamp of the last update"
        }
      },
      "required": ["adminPin"]
    },
    "ReportDoc": {
      "title": "Inspection Report Document",
      "description": "Monthly ATVSLD inspection report shared across workshop stations",
      "type": "object",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique report identifier"
        },
        "month": {
          "type": "string",
          "description": "Inspection report month (MM/YYYY)"
        },
        "reportDate": {
          "type": "string",
          "description": "Formatted date of inspection"
        },
        "dataJson": {
          "type": "string",
          "description": "Full serialized JSON representation of the report"
        },
        "updatedAt": {
          "type": "string",
          "description": "ISO timestamp of last update"
        }
      },
      "required": ["id", "month", "dataJson"]
    },
    "DeletedReportDoc": {
      "title": "Deleted Report Tombstone",
      "description": "Records report IDs that have been explicitly deleted to prevent resurrection",
      "type": "object",
      "properties": {
        "id": {
          "type": "string",
          "description": "Deleted report identifier"
        },
        "month": {
          "type": "string",
          "description": "Inspection report month (MM/YYYY)"
        },
        "deletedAt": {
          "type": "string",
          "description": "ISO timestamp of deletion"
        }
      },
      "required": ["id"]
    }
  },
  "firestore": {
    "/app_settings/{settingId}": {
      "schema": "SystemConfig",
      "description": "Global application configuration documents including admin PIN"
    },
    "/reports/{reportId}": {
      "schema": "ReportDoc",
      "description": "All monthly safety inspection reports shared across all computers"
    },
    "/deleted_reports/{reportId}": {
      "schema": "DeletedReportDoc",
      "description": "Records of deleted reports to ensure deleted items are never restored"
    }
  }
}
